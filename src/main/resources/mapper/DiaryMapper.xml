<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.growgreen.diary.DiaryMapper">

    <insert id="insDiary">
        INSERT INTO p_diary
        (title, ctnt)
        VALUES
        (#{title},#{ctnt})
    </insert>

    <delete id="delDiary">
        DELETE FROM p_diary
        WHERE idiary = #{idiary}
    </delete>

    <update id="updDiary">
        UPDATE p_diary
        SET title = #{title},
        ctnt = #{ctnt}
        WHERE idiary = #{idiary}
    </update>

    <select id="selDiaryAll">
        SELECT DATE_FORMAT(created_at, '%Y.%m.%d') createdAt, title, ctnt
        FROM p_diary
    </select>
    <select id="selDiaryById">
        SELECT DATE_FORMAT(created_at, '%Y.%m.%d') createdAt, title, ctnt
        FROM p_diary
        WHERE idiary = #{idiary}
    </select>
</mapper>